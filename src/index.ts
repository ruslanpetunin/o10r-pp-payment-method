import type { PaymentMethodFactory } from 'orchestrator-pp-flow';

const paymentMethodFactory: PaymentMethodFactory = async (initData, projectSettings) => {
    return [];
};

export default paymentMethodFactory;